.. |mydef| replace:: a substitution definition
.. role:: custom (substitution-reference)

Now we can refer to |mydef| using a substitution reference or to
`mydef`:substitution-reference: using a substitution-reference role or
to `mydef`:custom: using a custom role based off
substitution-reference.
