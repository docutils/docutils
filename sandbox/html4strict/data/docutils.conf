# Local docutils conf file,
# the content can be copied to a users ~/.doctutils file for personalized defaults

[html4strict writer]
# These entries affect HTML output when the html4strict writer is used:

# the default style-sheet
stylesheet: html4css2.css
embed-stylesheet: no
