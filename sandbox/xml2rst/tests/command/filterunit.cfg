# Tests for the `xml2rst.py` command itself

inputExtension = .xml
argumentExtension = .arg
expectedExtension = .emp
expectedTreeExtension = .d
arguments = $<

command = $<fixtureDirectory>/../../xml2rst.py
