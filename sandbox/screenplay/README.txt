This is the working directory for making a Screenplay directive which
uses parts delayed processing to inject a transform which takes a
raw screenplay as specified on the web and format it into nodes and
node classes which can then be styled after various writer transforms.

