# Tests with the HTML writer

command = ../../rstdiff.py

inputExtension = .old.rst
inputExtension = .new.rst
inputCount = 2
expectedExtension = .html

options = --writer=html --no-doc-title
arguments = $1< $2<
