# Simple tests with the HTML writer

command = ../../rstdiff.py

inputDirectory = ../simple/input
inputExtension = .old.rst
inputExtension = .new.rst
inputCount = 2
expectedExtension = .html

options = --writer=html --no-doc-title --error-encoding=iso-8859-15
arguments = $1< $2<
