# Tests for options `--new` / `--old`

command = ../../rstdiff.py

inputExtension = .rst
optionExtension = .opt
expectedExtension = .xml

options = --writer=xml --indents --no-doctype
arguments = $< $<
