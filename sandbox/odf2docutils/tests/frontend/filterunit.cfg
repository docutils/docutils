command = ../../odf2docutils.py
# `options` may not be used since it is used in XSLT tests
optionExtension = .opt
argumentExtension = .arg
inputExtension = .odt
expectedExtension = .xml
errorExtension = .err
exitExtension = .ex
errorFilter = head -n 1
expectedFilter = ../expectedFilter
